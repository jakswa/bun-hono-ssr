{
  "name": "bun-hono-ssr",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "start:prod": "bun build/index.js",
    "db:migrate": "bun src/tasks/migrate.ts",
    "db:types": "bun bun-sqlgen generate 'src/**/*.ts' --migrations db/migrations --out src/db/queries/queries.gen.d.ts",
    "typecheck": "tsc --noEmit",
    "app:build": "bun scripts/build.ts",
    "build": "bun run db:types && bun run typecheck && bun test && bun run app:build"
  },
  "dependencies": {
    "eta": "^3.5.0",
    "hono": "^4.10.7"
  },
  "devDependencies": {
    "@ilbertt/bun-sqlgen": "^0.3.1",
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
